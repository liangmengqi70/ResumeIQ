# ResumeIQ 开发交接说明

更新：2026-09-13。本文件记录已确认的 MVP 需求，供桌面「ResumeIQ开发」项目使用。与本目录的 `schema.sql` 配套。本文不是代码，也不代表数据库实际结构已核验。

## 产品定位与范围

- 面向求职者的 AI 简历诊断工具，首要用户是找实习的在校生及应届生，但不排除其他求职者。
- 必须结合一份目标岗位 JD 诊断当前简历；不是预测面试结果，也不评价用户本人是否有能力。
- 第一版使用一次 Prompt 约束 AI 模拟 HR 初筛，不使用 RAG、Agent、数字评分、在线改写编辑、报告下载或付费系统。
- 按功能逐步开发和测试：登录/游客入口 → 简历上传 → JD → 背景调查 → AI 分析 → 游客解锁 → 报告 → 历史记录/个人中心。不要一次性开发整站。

## 页面与体验

- 已有 Figma Make 生成的登录、简历上传、JD、背景调查等页面；后续页面也已有设计讨论。开发时以用户提供的最终截图为视觉依据，统一 Logo、字体、线条、间距和按钮样式。不要在没有设计依据时自行扩展页面。
- 登录：+86 手机号验证码。首次登录自动建账号、生成默认昵称和头像。开发环境用固定测试验证码，绝不能暴露在公网正式环境。未登录页面右上角显示「登录」，允许「游客体验」。登录会话建议保持 30 天，主动退出立即失效。
- 登录后头像菜单：历史记录、个人中心、退出登录。个人中心为右侧抽屉，仅有可修改的头像和昵称、只读且脱敏的手机号、只读注册时间；头像支持 JPG/PNG、5 MB、简单裁剪、恢复默认。
- 游客可完成分析。报告生成后只看到模糊占位与登录弹窗，后端在登录鉴权前绝不能返回完整报告内容。登录后绑定当前浏览器的游客诊断并解锁。游客同一会话最多生成一份报告，失败后的重试不计；后端还需限流。
- 游客草稿在最后一次操作后保留 24 小时；游客报告生成后保留 10 天。过期未查看则不消耗免费机会。绑定账号后长期保存，直到用户主动删除。

## 输入与诊断流程

- 简历：每次一份 PDF/DOC/DOCX，最大 10 MB、最多 5 页；不支持扫描件、纯图片、加密、损坏、空白或无法解析的文件。解析成功才能进入下一步。不展示提取文本预览。原始文件私有存储，提取的文字存数据库。历史记录按原文件名展示，上传时间精确到分钟。
- JD：必须粘贴文本，100～5000 字，一次一份岗位；支持可编辑的示例 JD，示例诊断应标识为演示。清洗只去除无效格式，不改变岗位要求。
- 背景调查：沿用用户现有 UI，包含当前身份、投递情况、求职困难多选、自由说明；不擅自改动题目。底部按钮「开始 AI 诊断」。
- AI 等待页标题为「AI HR 正在分析中」，无副标题；展示一条当前状态，不展示进度百分比或预计时长。关闭网页后后台继续；刷新恢复同一任务，不重复调用。失败显示「分析未完成」，允许重试或返回修改，不消耗免费机会。

## 报告内容

- 报告为纵向长页，三板块：① AI HR 初筛结论；② 为什么是这个结论（问题及修改建议）；③ 你的简历优势。底部「返回首页」。不单列优化清单。
- 结论仅三档，展示顺序：推荐 → 存在顾虑 → 不推荐。每项前一个圆点；只有当前项使用对应绿/橙/红色，其他项的圆点和文字均为灰色；无卡片、外框、滑轨或数字分数。
- 问题按高/中/低影响排序；影响标签只包住三个字，不横向拉满。每项说明：问题标题、大白话解释、简历原文证据、对应 JD 要求、为何影响初筛、具体修改建议、参考改写。参考改写统一标「仅供参考」，不得捏造事实或数字。
- 优势只展示已在简历中明确体现、且与 JD 有关的内容及其价值；不重复放修改建议。
- Prompt 输出固定 JSON：`conclusion`、`conclusion_summary`、`issues`、`strengths`。结论值为 `recommended` / `concerned` / `not_recommended`；问题最多 5 项，优势最多 4 项；无数字评分。模型输出须经后端结构及内容校验。

## 历史记录与免费机会

- 历史记录不单独建表，只查询已绑定账号、成功生成、未删除的诊断报告。页面显示原文件名、目标岗位、结论、上传时间、诊断完成时间；只提供查看报告与删除，不提供 JD 原文查看或简历下载。需要空状态和删除确认。
- 报告生成、游客登录和绑定均不消耗免费机会；**登录用户第一次成功获取完整报告时**才消耗一次。重复查看不重复消耗。已用过免费机会的账号不能免费解锁第二份报告。使用记录应与首次查看在同一事务中处理。
- 删除历史记录后，报告不可再查看，但「免费机会已用」事实仍须保留；这点与当前外键结构的物理删除存在冲突，开发该功能前必须核查并确定软删除/清理策略。

## 技术架构

- 暂定：Next.js + TypeScript（页面和 API）、MySQL、数据库访问层、私有文件存储、独立常驻 Worker、OpenAI API。
- Worker 从 MySQL 领取任务并调用模型，用户关闭页面后分析继续；第一版不额外引入 Redis/RAG/Agent/支付/多模型选择。
- 先使用单个可配置的 OpenAI 模型，开发阶段偏向低成本模型，但正式模型应通过测试比较后确定。API Key 仅放服务器端。简历文字、JD、调查答案会送往 OpenAI，隐私说明须如实披露。
- 每次模型调用（包括失败和重试）在 `ai_usage_records` 中记录模型、Token 和调用时价格快照。当前无用户付费功能。

## 数据库交接注意事项

- 配套 `schema.sql` 包含 **9 张英文表**：`users`、`guest_sessions`、`resume_files`、`job_descriptions`、`background_surveys`、`diagnosis_tasks`、`ai_usage_records`、`diagnosis_reports`、`free_usage_records`。
- `schema.sql` 是建表基线，不是迁移脚本；已有表上的 `CREATE TABLE IF NOT EXISTS` 不会更新字段。此前在 DBeaver 分次执行和修订过 SQL，不能仅凭文件认定真实数据库完全一致。开发前先用 `SHOW CREATE TABLE` 或结构比对核验九表，发现差异后再制定有针对性的迁移，勿盲目重跑/删除。
- 当前脚本中的游客会话、免费使用记录与删除策略仍需和实际业务实现共同校验。不要把数据库密码、OpenAI 密钥或真实手机号写入仓库。

## 开发起点

正式开发目录：`C:\Users\lmq20\Desktop\ResumeIQ\ResumeIQ`。本说明与 `schema.sql` 应复制到该目录。先只实现登录与游客入口，逐项测试通过后再进入简历上传。
